import { LimitToDotsPipe } from './limit-to-dots.pipe';

describe('LimitToDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitToDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
